algoritmo "Salário"

var
   salario, aumento: real

inicio
   Escreval("Informe o seu Salário: ")
   leia(salario)
   aumento <- salario + (salario * 0.3)
   Escreval("O seu novo Salário com 30% de aumento, será de: R$", aumento, " Reais.")

fimalgoritmo
